type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  createdAt: Int!
  updatedAt: Int
}

input UserInput {
  name: String!
  email: String!
  password: String!
}

type Mutation {
  register(input: UserInput!): User!
}
