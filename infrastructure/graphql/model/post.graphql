type Post {
  id: ID!
  title: String!
  content: String!
  created_at: Int!
  updated_at: Int!
}

type Query {
  findAllPost: [Post]
}

type Mutation {
  createPost(title: String!, content: String!): Post
}
